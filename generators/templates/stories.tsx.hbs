import { Story, Meta } from '@storybook/react/types-6-0'

import {{properCase name}} from '.'

export default {
  title: '{{properCase name}}',
  component: {{properCase name}}
} as Meta

export const Basic: Story = (args) => <{{properCase name}} {...args} />
